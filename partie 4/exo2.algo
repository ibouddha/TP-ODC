Algorithme est_bissextile(annee)
    # Entrées : 
    # - annee (entier)
    # Sortie :
    # - bissextile (booléen)
    
    bissextile <- Faux
    SI (annee modulo 4 est égal à 0 ET annee modulo 100 n'est pas égal à 0) OU (annee modulo 400 est égal à 0) ALORS
        bissextile <- Vrai
    FIN SI
    Retourner bissextile
Fin Algorithme
